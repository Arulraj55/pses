# Backend env example (safe to commit)
# Copy to `server/.env` if you prefer per-service envs, or use the root `.env`.

# Database
MONGODB_URI=mongodb://127.0.0.1:27017/pses

# Auth
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

# CORS (comma-separated; use * only in dev)
CORS_ORIGIN=http://localhost:5173

# External APIs
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash-latest
YOUTUBE_API_KEY=
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=

# Services
ML_SERVICE_URL=http://127.0.0.1:8000

# Server
PORT=5000